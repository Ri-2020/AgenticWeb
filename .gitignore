# OS junk
.DS_Store

# Python / virtualenv
__pycache__/
.venv/
.env
.env.local
*.pyc

# Node / Next.js
node_modules/
.next/
*.log

# Build artifacts
dist/
build/

# Personal notes — not for the repo
INTERVIEW_*.md

# Credentials — block service account key files only
# Do NOT block package.json, tsconfig.json, firebase.json, etc.
*-credentials.json
*-key.json
*-keyfile.json
cred_keys/
# uv.lock is intentionally tracked — Docker builds depend on it for reproducible installs